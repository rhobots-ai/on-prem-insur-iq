{{- if .Values.celery.beat.enabled -}}
# Celery Beat — singleton. Recreate strategy ensures old pod terminates before
# new pod starts (DatabaseScheduler relies on no concurrent scheduler instances).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "insur-iq.fullname" . }}-celery-beat
  labels:
    {{- include "insur-iq.labels" . | nindent 4 }}
    app.kubernetes.io/component: celery-beat
spec:
  replicas: 1
  revisionHistoryLimit: 2
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "insur-iq.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: celery-beat
  template:
    metadata:
      labels:
        {{- include "insur-iq.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: celery-beat
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.celery.podAnnotations }}{{ toYaml . | nindent 8 }}{{- end }}
    spec:
      serviceAccountName: {{ include "insur-iq.sa.celery" . }}
      securityContext:
        {{- include "insur-iq.podSecurityContext" . | nindent 8 }}
      containers:
        - name: beat
          image: {{ include "insur-iq.image.backend" . }}
          imagePullPolicy: {{ .Values.image.backend.pullPolicy }}
          command:
            - celery
            - -A
            - config
            - beat
            - --loglevel=INFO
            - --scheduler=django_celery_beat.schedulers:DatabaseScheduler
          envFrom:
            {{- include "insur-iq.backendEnvFrom" . | nindent 12 }}
          env:
            {{- include "insur-iq.backendDerivedEnv" . | nindent 12 }}
          resources: {{ toYaml .Values.celery.beat.resources | nindent 12 }}
          securityContext:
            {{- include "insur-iq.containerSecurityContext" . | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 64Mi
{{- end }}
